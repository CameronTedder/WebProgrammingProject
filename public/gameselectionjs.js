const featuredGameCard = document.getElementById("featured-game");

// Add a click event listener to the featured game
featuredGameCard.addEventListener("click", () => {
  // Redirect to the game page (you can implement this page later)
  window.location.href = "gamepage.html";
});